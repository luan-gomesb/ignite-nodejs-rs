describe("createCategoryUseCase", () => {
  it("o que estamos testando", () => {
    const soma = 2 + 2;
    expect(soma).toBe(4);
  })

  it("o que estamos testando 2", () => {
    const soma = 3 + 2;
    expect(soma).toBe(4);
  })



})